# 前置验证结论汇总

> 本文件是阶段一三个探针的产物。顶部决策表汇总三项结论，并给出阶段二准入判定；下方依次是各探针的完整实测记录。

## 决策表

| 验证 | 问题 | 结论 | 对阶段二的影响 |
|---|---|---|---|
| 2 | Raw Input 数据完备性（非焦点下能否拿到鼠标增量与键盘 scancode） | ✅ **通过**——1427 条非探针前台鼠标事件，1343 条（94%）有非零增量；键盘 scancode、三键、滚轮 ±120、Alt+Tab 组合键全部完整 | 采集方案可用。❌ 则采集方案作废 |
| 1 | 焦点小窗键盘抑制（后台进程能否夺取前台、夺取后是否漏键） | ✅ **通过**——策略 A（朴素 `SetForegroundWindow`）失败，策略 B（`AttachThreadInput` 绕行）成功；特征串 `qqqqqwwwww` 零泄漏 | 抑制层**必须用 `AttachThreadInput` 绕行**。❌ 则必须改用 WH_KEYBOARD_LL（青骓风险）或内核驱动 |
| 4 | 设备侧 UHID 自建（手机零安装能否有真实 HID 光标） | ✅ **通过**——`/dev/uhid` 打开，CREATE2 以 **4376 字节**一次通过，系统光标真实渲染并移动（`REL_X/REL_Y=+3`），`app_process` 4/4 干净运行 | 手机侧零安装成立。❌ 则退回装 APK，放弃"手机零安装" |

> 另：验证 3（adb 通路）此前已通过，记录在 spec 第 5 节，不在本表覆盖范围内。

## 准入判定

**三项探针全部通过 → 进入阶段二（端到端行走骨架）。**

阶段二设计必须遵守的两条约束（均由实测确立，非推演）：

1. **抑制机制必须使用 `AttachThreadInput` 绕行**（见验证 1）。朴素的 `SetForegroundWindow` 在目标机（Win11 26200）上实测抢不到前台，不可用。
2. **手机侧确认为零安装**（见验证 4）。`adb push` + `app_process` + 写 `/dev/uhid` 即可，无 APK、无图标、无权限弹窗、无 Shizuku——spec 第 6 节架构图无需引入 APK。

阶段二的计划须在以上结论基础上编写；spec 第 6、8 节无需改写（两条失败分支均未触发）。

---

## 验证 2：Raw Input 数据完备性

**结论**：✅ **通过**

| 检查项 | 结果 |
|---|---|
| `RAWMOUSE` 结构体大小 | **24** ✅（`_pad` 显式填充生效） |
| 非焦点时收到鼠标相对增量 `lLastX`/`lLastY` | **是** ✅ |
| 非焦点时收到键盘 scancode | **是** ✅ |
| 多指点设备干扰 | **无** ✅ |

**实测数据**（日志 5325 行，来自 `probes/01-rawinput/rawinput-probe.log`）：

- **非探针前台下的鼠标事件：1427 条，其中 1343 条有非零增量（94%）**
  ← 这是本探针的核心问题，答案明确为"能"
- 非探针前台下的键盘事件：90 条
- 实测覆盖到的前台窗口：`Notepad`、`任务切换`（Alt+Tab）、`Everything`、`搜索`、`(untitled)`
- 设备句柄：鼠标仅 `1004C` 一个，键盘仅 `10045` 一个——**本机那两个虚拟显示驱动（`GameViewer`、`Sharing Monitor`）没有产生额外的指点设备**

**逐项判定**：

| 计划判定项 | 实测 | 结论 |
|---|---|---|
| `RAWMOUSE size` = 24 | 24 | ✅ |
| 步骤 2（焦点在记事本）有非零增量 | 1343/1427 条非零 | ✅ |
| 行尾前台窗口名证明非焦点 | `*abcdef AA - Notepad` 等 | ✅ |
| 步骤 3 六个字母 + Shift | vkey 65–70 全部命中，Shift(vkey 16) 14 次 | ✅ |
| 步骤 4 滚轮为 ±120 倍数 | 16× -120、4× +120，**非 120 倍数 0 条** | ✅ |
| 步骤 5 左/右/中键 | 左 `0x0001`×33 / `0x0002`×32，右 `0x0004`×2 / `0x0008`×2，中 `0x0010`×9 / `0x0020`×9 | ✅ |

**实测备注**：

1. **`ForegroundTitle()` 是每条 `WM_INPUT` 都调一次 `GetWindowText`**——高频鼠标移动下这是明显的性能噪音。产品化时应该改成事件驱动（监听 `EVENT_SYSTEM_FOREGROUND`）或降频采样，而不是逐事件查询。
2. **本次未出现扩展键（`0xE000`）**——测试期间没按方向键/小键盘等。scancode 的 E0 处理分支**未被实测覆盖**，产品化时需另外验证。
3. **滚轮无触控板中间值**——用户用的是真实鼠标，所以滚动严格是 ±120 的倍数。若将来在触控板上测，需按实际情况判定。
4. **Alt+Tab 被完整捕获**（Tab 的 vkey=9 出现 3 次，且带 Alt 的 vkey=18 配对）——说明组合键在非焦点下也能拿到完整序列，这对将来透传系统热键是好消息。
5. 日志为 **UTF-8 无 BOM**，PowerShell 5.1 的 `Get-Content` 直接读中文窗口标题可能乱码；用 `rg`/`awk` 读正常。

---

## 验证 1：焦点小窗键盘抑制

**结论**：✅ **通过**

| 检查项 | 结果 |
|---|---|
| **策略 A**（朴素 `SetForegroundWindow`） | ❌ **失败** |
| **策略 B**（`AttachThreadInput` + `SetForegroundWindow` + detach） | ✅ **成功** |
| 按键泄漏 | ✅ **无**——特征串 10 个字符，记事本中一个都没出现 |

**结论性影响（阶段二必须遵守）**：

> **产品实现的抑制层必须使用 `AttachThreadInput` 绕行，朴素的 `SetForegroundWindow` 不可用。**

Windows 的前台锁定规则**实测生效**：后台进程调用 `SetForegroundWindow` 抢不到前台。这条此前只是从文档推演，现在是在目标机器（Win11 26200）上实测确认的。若不测这一步，阶段二很可能写出"本地测试能过、真实场景抢不到前台"的代码——因为本地测试时窗口本来就有焦点，朴素的 `SetForegroundWindow` 永远返回成功，问题被完全掩盖。

**同时排除的重大风险**：若策略 A、B **均**失败，焦点抑制方案整体作废，只能退回 `WH_KEYBOARD_LL` 全局钩子——那会直接撞上本机网易青骓的键盘记录器启发式规则。此路已确认可走。

**测试方法说明（这一条决定结论是否可信）**：本轮采用**隔离测法**——倒计时结束后**先等面板确认夺取成功，再开始打特征串**。

之所以必须这样做：开发期间的非受控实测曾观察到记事本疑似多出 3 个 `d`，当时无法区分两种成因——
- **竞态**：倒计时那 3 秒内前台仍在记事本，此时按键本就该进记事本 → 属预期
- **持续泄漏**：夺取成功后仍漏键 → 机制有洞、方案需重做

隔离测法用"确认夺取完成后才开始输入"把观测窗口切开，10 个特征字符零泄漏，证明**先前的 3 个 `d` 是竞态，不是漏洞**。

**未在本轮单独回报的项（不构成结论缺口）**：

- `ClipCursor` 是否成功锁定光标、焦点是否成功还原——本轮未逐项回报。但解除流程完整走通（用户随后恢复正常输入），说明释放路径有效。这两项在阶段二产品化时会被再次覆盖。
- 系统级热键（`Win` 键、`Ctrl+Alt+Del`）的泄漏情况未测。按设计它们会被操作系统截走，属已知可接受限制（spec 第 8 节）。
- `Win` 键的 0xE0 扩展键路径（验证 2 遗留）同样未覆盖。

---

## 验证 4：设备侧 UHID 自建（零安装）

**结论**：✅ **通过**

| 检查项 | 结果 |
|---|---|
| `open /dev/uhid` | ✅ 成功（SELinux Enforcing 下 shell uid 未受阻） |
| **CREATE2 被内核接受的字节数** | ✅ **4376** —— 一次通过，未回退到 4102 / 4380 |
| 手机是否出现光标 | ✅ 是 |
| 光标是否按预期移动 | ✅ 是。`getevent` 确认 `REL_X/REL_Y = +3`；截图追踪轨迹 `(2150,1621) → (2218,1689) → (2401,1872)` |
| `app_process` 稳定性 | ✅ **4/4 次干净运行** |
| `d8` 是否需要 `--lib` | ✅ 不需要（预判正确，未触发回退） |

**结论性影响（阶段二必须遵守）**：

> **手机侧零安装成立。** `adb push` + `app_process` + 写 `/dev/uhid` 可在目标机上（小米 HyperOS 3.0 / Android 16，未 root，SELinux Enforcing）稳定造出真实 HID 鼠标。**无 APK、无图标、无权限弹窗、无 Shizuku。**

**实测备注**：

1. **`build.ps1` / `run.ps1` 必须存为 UTF-8 **with BOM**。** Windows PowerShell 5.1 会把无 BOM 的 UTF-8 中文注释按 GBK 解读，**静默破坏解析**（表现为 `$zip` 变量变成 null，且不报明确错误）。这个坑在阶段二会重复踩，务必固化。
2. **r8 8.9.35 实际约 17 MB**，不是计划里估的 30–40 MB。
3. **光标位置跨设备销毁/重建而持续存在** —— 销毁虚拟设备再重建后，指针不会回到原点，而是留在上次的位置。**这直接印证了 spec 第 7 节「推角落归零」设计的必要性**：每次进入 `TAKEOVER` 时必须先把光标硬顶到 `(0,0)` 完成归零，否则虚拟光标模型与系统光标会永久错位。
4. 创建设备后**未读 `UHID_START` 事件即开始写 input 报告**，内核并未拒绝——说明该事件的读取不是必需的（但阶段二仍建议读取，以便感知设备就绪与销毁）。
5. `-Mode create` 的 `+3/+3` 每 200ms 会让光标行至屏幕边缘后被系统钳住，属预期行为，非故障。

